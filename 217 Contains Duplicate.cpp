#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {

    }
};

int main() {
    Solution s;
    vector<int> v;
    cin >> i;
    while (i) {
        v.push_back(i);
        cin >> i;
    }
    cout << s.containsDuplicate(v) << endl;
    return 0;
}
